{
  "name": "sparkles-detergents",
  "version": "1.0.0",
  "description": "Sparkles Detergents — backend + frontend",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "backend": "cd backend && npm run start",
    "install:backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "firebase-admin": "^13.7.0",
    "path": "^0.12.7"
  }
}
